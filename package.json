{
  "name": "mate-phone-catalog-api",
  "private": true,
  "scripts": {
    "start": "json-server db.json --host 0.0.0.0 --port $PORT --static ./public"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}